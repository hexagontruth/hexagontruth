
import Player from './player.js';

export default class Player2D extends Player {
  constructor(page, name, canvas, config) {
    super(page, name, canvas, config);
  }

  clear() {

  }

  setup() {
  }

  run() {
  }
}
